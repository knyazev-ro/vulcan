### 🛠 Sandbox Setup & SQL Examples

Для запуска тестов в директории `./sandbox` выполни следующие шаги:

#### 1. Подготовка окружения

```bash
cd ./sandbox

# Сборка и запуск контейнера с тестовыми данными
docker build -t vulcan-db-test .
docker run -d --name vulcan-db -p 25432:5432 vulcan-db-test

# Запуск тестов интеграции
go run .